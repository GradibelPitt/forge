# Garrosh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the PH01 mythic planeswalker `加尔鲁什`, its legendary Equipment token `影之哀伤`, both supplied artworks, the requested loyalty abilities, Chinese display text, tests, and deployed runtime resources.

**Architecture:** Keep the implementation entirely in Forge's existing card DSL. Reuse Mabel's `ChangesZone` plus `TokenScript` pattern for the Equipment, native `DamageAll` for the `+1`, and the official `b_2_2_zombie_decayed` token for the `-1`; no Java change is required. Store the main art as a PH01 dynamic-frame art crop and the Equipment art under the token script key `shadowmourne`.

**Tech Stack:** Forge card scripts, Python `unittest`, Pillow image assertions, PowerShell `System.Drawing` for deterministic JPEG conversion/cropping, Forge DIY lint/install scripts.

---

### Task 1: Add the failing Garrosh contract

**Files:**
- Create: `custom/tests/test_garrosh.py`

- [x] **Step 1: Write the failing contract test**

```python
import unittest
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
CARD = ROOT / "cards" / "multicolor" / "加尔鲁什.txt"
TOKEN = ROOT / "tokens" / "shadowmourne.txt"
EDITION = ROOT / "editions" / "Placeholder_Set.txt"
CARD_ART = ROOT / "cards" / "pictures" / "PH01" / "加尔鲁什.artcrop.jpg"
TOKEN_ART = ROOT / "tokens" / "pictures" / "shadowmourne.jpg"
CARD_ART_BACKUP = ROOT / "tools" / "card-artwork" / "codex-clipboard-ce7a6eb3-06de-49dc-88b4-38729c7a5dd0.png"
TOKEN_ART_BACKUP = ROOT / "tools" / "card-artwork" / "codex-clipboard-c13c17c0-981d-453a-94fa-ee7817d9e667.png"
ZH_CN = ROOT.parent / "forge-gui" / "res" / "languages" / "cardnames-zh-CN.txt"


class GarroshContractTest(unittest.TestCase):
    def read_card(self):
        self.assertTrue(CARD.is_file(), CARD)
        return CARD.read_text(encoding="utf-8")

    def test_planeswalker_characteristics(self):
        text = self.read_card()
        self.assertIn("Name:加尔鲁什", text)
        self.assertIn("ManaCost:2 B R", text)
        self.assertIn("Types:Legendary Planeswalker Garrosh", text)
        self.assertIn("Loyalty:5", text)

    def test_enters_and_creates_shadowmourne(self):
        text = self.read_card()
        self.assertIn(
            "T:Mode$ ChangesZone | Origin$ Any | Destination$ Battlefield | "
            "ValidCard$ Card.Self | Execute$ TrigShadowmourne",
            text,
        )
        self.assertIn(
            "SVar:TrigShadowmourne:DB$ Token | TokenScript$ shadowmourne",
            text,
        )

    def test_plus_one_damages_each_creature(self):
        text = self.read_card()
        self.assertIn(
            "A:AB$ DamageAll | Cost$ AddCounter<1/LOYALTY> | Planeswalker$ True | "
            "ValidCards$ Creature | NumDmg$ 1",
            text,
        )

    def test_minus_one_creates_a_decayed_zombie(self):
        text = self.read_card()
        self.assertIn(
            "A:AB$ Token | Cost$ SubCounter<1/LOYALTY> | Planeswalker$ True | "
            "TokenScript$ b_2_2_zombie_decayed | TokenAmount$ 1 | TokenOwner$ You",
            text,
        )

    def test_shadowmourne_has_the_requested_equipment_rules(self):
        self.assertTrue(TOKEN.is_file(), TOKEN)
        text = TOKEN.read_text(encoding="utf-8")
        self.assertIn("Name:影之哀伤", text)
        self.assertIn("ManaCost:no cost", text)
        self.assertIn("Types:Legendary Artifact Equipment", text)
        self.assertIn("K:Equip:4", text)
        self.assertIn(
            "S:Mode$ Continuous | Affected$ Creature.EquippedBy | AddPower$ 4 | "
            "AddToughness$ 3 | AddKeyword$ Double Strike & Menace",
            text,
        )

    def test_registration_art_and_backups_exist(self):
        edition = EDITION.read_text(encoding="utf-8")
        self.assertIn("57 M 加尔鲁什 @Custom", edition)
        card_lines = edition.split("[cards]", 1)[1].splitlines()
        collector_numbers = [line.split(maxsplit=1)[0] for line in card_lines if line.strip()]
        self.assertEqual(len(collector_numbers), len(set(collector_numbers)))
        for path in (CARD_ART, TOKEN_ART, CARD_ART_BACKUP, TOKEN_ART_BACKUP):
            self.assertTrue(path.is_file(), path)

    def test_art_is_landscape_rgb_jpeg(self):
        from PIL import Image

        with Image.open(CARD_ART) as image:
            self.assertEqual((816, 600), image.size)
            self.assertEqual("RGB", image.mode)
            self.assertAlmostEqual(1.37, image.width / image.height, delta=0.02)
        with Image.open(TOKEN_ART) as image:
            self.assertEqual((400, 292), image.size)
            self.assertEqual("RGB", image.mode)
            self.assertAlmostEqual(1.37, image.width / image.height, delta=0.02)

    def test_zh_cn_text_matches_the_requested_oracle(self):
        expected = (
            "加尔鲁什|加尔鲁什|传奇鹏洛客～加尔鲁什|"
            "当加尔鲁什进场时，派出传奇衍生神器影之哀伤，其为无色武具，且具有"
            "「佩带此武具的生物得+4/+3且具有连击与威慑异能」与佩带{4}。\\n"
            "+1：加尔鲁什对每个生物各造成1点伤害。\\n"
            "-1：派出一个2/2黑色，具败朽异能的灵俑衍生生物。"
        )
        self.assertIn(expected, ZH_CN.read_text(encoding="utf-8").splitlines())


if __name__ == "__main__":
    unittest.main()
```

- [x] **Step 2: Run the new test and verify RED**

Run: `python -m unittest tests.test_garrosh -v`

Expected: FAIL because the Garrosh card, Shadowmourne token, PH01 #57 row, images, backups, and zh-CN row do not exist.

### Task 2: Implement the card, token, set row, and localization

**Files:**
- Create: `custom/cards/multicolor/加尔鲁什.txt`
- Create: `custom/tokens/shadowmourne.txt`
- Modify: `custom/editions/Placeholder_Set.txt`
- Modify: `forge-gui/res/languages/cardnames-zh-CN.txt`

- [x] **Step 1: Add the minimal Forge DSL implementation**

`custom/cards/multicolor/加尔鲁什.txt`:

```text
Name:加尔鲁什
ManaCost:2 B R
Types:Legendary Planeswalker Garrosh
Loyalty:5
T:Mode$ ChangesZone | Origin$ Any | Destination$ Battlefield | ValidCard$ Card.Self | Execute$ TrigShadowmourne | TriggerDescription$ 当加尔鲁什进场时，派出传奇衍生神器影之哀伤，其为无色武具，且具有「佩带此武具的生物得+4/+3且具有连击与威慑异能」与佩带{4}。
SVar:TrigShadowmourne:DB$ Token | TokenScript$ shadowmourne
A:AB$ DamageAll | Cost$ AddCounter<1/LOYALTY> | Planeswalker$ True | ValidCards$ Creature | NumDmg$ 1 | SpellDescription$ 加尔鲁什对每个生物各造成1点伤害。
A:AB$ Token | Cost$ SubCounter<1/LOYALTY> | Planeswalker$ True | TokenScript$ b_2_2_zombie_decayed | TokenAmount$ 1 | TokenOwner$ You | SpellDescription$ 派出一个2/2黑色，具败朽异能的灵俑衍生生物。
DeckHas:Ability$Token
Oracle:当加尔鲁什进场时，派出传奇衍生神器影之哀伤，其为无色武具，且具有「佩带此武具的生物得+4/+3且具有连击与威慑异能」与佩带{4}。\n[+1]: 加尔鲁什对每个生物各造成1点伤害。\n[-1]: 派出一个2/2黑色，具败朽异能的灵俑衍生生物。
```

`custom/tokens/shadowmourne.txt`:

```text
Name:影之哀伤
ManaCost:no cost
Types:Legendary Artifact Equipment
K:Equip:4
S:Mode$ Continuous | Affected$ Creature.EquippedBy | AddPower$ 4 | AddToughness$ 3 | AddKeyword$ Double Strike & Menace | Description$ 佩带此武具的生物得+4/+3且具有连击与威慑异能。
Oracle:佩带此武具的生物得+4/+3且具有连击与威慑异能。\n佩带{4}
```

Append `57 M 加尔鲁什 @Custom` to PH01 and append the exact expected zh-CN row from the test. PH01 #56 is already occupied by `暗影收割者安度因`.

- [x] **Step 2: Run the focused test**

Run: `python -m unittest tests.test_garrosh -v`

Expected: card, token, edition, and localization assertions pass; artwork assertions remain red until Task 3.

### Task 3: Import the two supplied artworks

**Files:**
- Create: `custom/tools/card-artwork/codex-clipboard-ce7a6eb3-06de-49dc-88b4-38729c7a5dd0.png`
- Create: `custom/tools/card-artwork/codex-clipboard-c13c17c0-981d-453a-94fa-ee7817d9e667.png`
- Create: `custom/cards/pictures/PH01/加尔鲁什.artcrop.jpg`
- Create: `custom/tokens/pictures/shadowmourne.jpg`

- [x] **Step 1: Back up the original images without changing them**

```powershell
Copy-Item -LiteralPath 'C:\Users\Marsh\AppData\Local\Temp\codex-clipboard-ce7a6eb3-06de-49dc-88b4-38729c7a5dd0.png' -Destination 'tools\card-artwork\codex-clipboard-ce7a6eb3-06de-49dc-88b4-38729c7a5dd0.png'
Copy-Item -LiteralPath 'C:\Users\Marsh\AppData\Local\Temp\codex-clipboard-c13c17c0-981d-453a-94fa-ee7817d9e667.png' -Destination 'tools\card-artwork\codex-clipboard-c13c17c0-981d-453a-94fa-ee7817d9e667.png'
```

- [x] **Step 2: Convert the main image and crop the token image deterministically**

Use `System.Drawing` to save the full 816×600 Garrosh image as RGB JPEG and crop the 400×400 Shadowmourne source to the centered 400×292 rectangle `(0,54,400,292)`, preserving the weapon while removing the source's white top and bottom bands. Save both at JPEG quality 95.

- [x] **Step 3: Run the focused test and verify GREEN**

Run: `python -m unittest tests.test_garrosh -v`

Expected: all 8 tests pass.

### Task 4: Validate, document, deploy, and prove the deployed files

**Files:**
- Modify: `custom/CARDS.md`
- Modify: `custom/VERIFICATION.md`

- [x] **Step 1: Record the card in the authoritative docs**

Add PH01 #57 to `CARDS.md` with cost `{2}{B}{R}`, loyalty 5, ETB Shadowmourne, `+1` one damage to every creature, and `-1` one decayed 2/2 black Zombie. Add a dated `VERIFICATION.md` entry only after commands below actually pass.

- [x] **Step 2: Run lint and tests**

```powershell
python tools\lint_card.py cards\multicolor\加尔鲁什.txt
python tools\lint_card.py tokens\shadowmourne.txt
python -m unittest tests.test_garrosh -v
python -m unittest discover -s tests -p "test_*.py"
```

Expected: both lint commands have no errors, focused tests pass, and the full DIY suite passes.

- [x] **Step 3: Deploy and compare source/deployed hashes**

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\install_to_forge.ps1
```

Compare SHA-256 for the main script, token script, edition file, main art, and token art against their `%APPDATA%` or `%LOCALAPPDATA%` deployment destinations. All five source/deployed pairs must match.

- [x] **Step 4: Review the final diff without disturbing existing work**

Run `git diff --check` and targeted `git diff`/`git status --short`. Because `CARDS.md`, `VERIFICATION.md`, `Placeholder_Set.txt`, and `cardnames-zh-CN.txt` already contain unrelated uncommitted user work, do not stage or commit those overlapping files unless their prior changes can be excluded safely; report the local-commit limitation explicitly instead of capturing unrelated work.
