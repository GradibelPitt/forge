# 决战瞬间与图片键修复设计

将卡牌内部名称从含全角标点的 `决战！` 规范化为 `决战`，以稳定匹配 Forge 的 PH01 图片查找键；中文本地化继续显示“决战！”。牌张类别由法术改为瞬间，费用和效果不变。图片只保留实际使用的 `PH01/决战.artcrop.jpg`，版本登记改用内部名 `决战 @Custom`。

验证覆盖脚本类型、版本内部名、中文显示名与“瞬间”类别、实际图片路径、部署哈希及新客户端显示。

